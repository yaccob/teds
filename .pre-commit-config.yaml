repos:
  # Basic file hygiene
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: check-merge-conflict

  # Python code quality
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.6
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  # Type checking
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.7.0
    hooks:
      - id: mypy
        additional_dependencies: [types-PyYAML, types-jsonschema, types-requests]

  # Local tests (disabled during CI optimization - enable manually if needed)
  # - repo: local
  #   hooks:
  #     - id: pytest-unit
  #       name: Run unit tests with coverage
  #       entry: python -m pytest tests/unit --cov=teds_core --cov=teds --cov-branch --cov-report=term-missing --cov-fail-under=75 -q
  #       language: system
  #       pass_filenames: false
  #       always_run: true
  #
  #     - id: pytest-cli
  #       name: Run CLI tests
  #       entry: python -m pytest tests/cli --cov=teds_core --cov=teds --cov-branch --cov-report=term-missing -q
  #       language: system
  #       pass_filenames: false
  #       always_run: true
  #
  #     - id: demo-smoke
  #       name: Demo smoke test
  #       entry: bash -c 'python teds.py verify demo/sample_tests.yaml --output-level warning > /dev/null; [[ $? -eq 1 ]]'
  #       language: system
  #       pass_filenames: false
  #       always_run: true
